$ git clone https://{username}:ghp_{your PAT token}@gist.github.com/{gist ID}.git

$ python converter.py image.png {gist filename} "git add {gist filename}; git commit -m 'm'; git push" https://github.com/{username}
